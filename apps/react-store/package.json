{
  "name": "@react-monorepo/react-store",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "netlify deploy --dir=dist"
  },
  "nx": {
    "targets": {
      "deploy": {
        "dependsOn": ["build"]
      }
    }
  }
}
